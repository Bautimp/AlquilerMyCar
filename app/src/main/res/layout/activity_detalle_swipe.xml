<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <LinearLayout
    android:id="@+id/contenedor_botones"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:orientation="vertical"
    android:padding="20dp"
    android:layout_marginBottom="30dp">

        <com.google.android.material.button.MaterialButton
    android:id="@+id/btn_alquilar_actual"
    android:layout_width="match_parent"
    android:layout_height="65dp"
    android:text="Realizar Alquiler"
    android:textSize="18sp"
    android:textStyle="bold"
    android:textAllCaps="false"
    app:cornerRadius="15dp"
    android:backgroundTint="#FF9F43"
    android:textColor="#121212"
    android:onClick="abrirFormularioAlquiler" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_volver_atras"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="35dp"
            android:text="← Volver al Inicio"
            android:textColor="@color/primary_blue"
            app:strokeColor="@color/primary_blue"
            app:cornerRadius="10dp"
            android:onClick="volverAtras" />

</LinearLayout>

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/viewPagerVehiculos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/contenedor_botones"
        android:layout_marginBottom="10dp" />

</RelativeLayout>